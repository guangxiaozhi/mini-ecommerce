package com.guang.miniecommercebackend.entity;

//定义“参与者在会话中的身份”。Defines each participant's role in the conversation (customer, human agent, or bot).
public enum ChatParticipantRole {
    CUSTOMER,
    HUMAN_AGENT,
    BOT
}
